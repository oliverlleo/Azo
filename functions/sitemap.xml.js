const SUPABASE_URL='https://jjrsbbgnqfiezhokxbqz.supabase.co';
const SUPABASE_KEY='sb_publishable_8LlV4bOH3d_axQBQLlHVkA_arQl6nu-';
const SITE_ORIGIN='https://www.azocc.com.br';
const headers={apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,Accept:'application/json'};
const esc=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
async function obras(){const response=await fetch(`${SUPABASE_URL}/rest/v1/obras?published=eq.true&archived=eq.false&allow_index=eq.true&select=slug,updated_at&order=sort_order.asc`,{headers});if(!response.ok)throw new Error(`Supabase ${response.status}`);return response.json();}
export async function onRequest(){const staticUrls=['/','/servicos/','/projetos/','/obras/','/sobre/','/contato/'];try{const rows=await obras();const urls=[...staticUrls.map(path=>({loc:`${SITE_ORIGIN}${path}`})),...(rows||[]).map(row=>({loc:`${SITE_ORIGIN}/obras/${encodeURIComponent(row.slug)}/`,lastmod:row.updated_at}))];const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(item=>`  <url>\n    <loc>${esc(item.loc)}</loc>${item.lastmod?`\n    <lastmod>${esc(new Date(item.lastmod).toISOString())}</lastmod>`:''}\n  </url>`).join('\n')}\n</urlset>`;return new Response(xml,{headers:{'content-type':'application/xml; charset=UTF-8','cache-control':'public, max-age=300, s-maxage=900'}});}catch{const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${staticUrls.map(path=>`  <url><loc>${esc(`${SITE_ORIGIN}${path}`)}</loc></url>`).join('\n')}\n</urlset>`;return new Response(xml,{headers:{'content-type':'application/xml; charset=UTF-8','cache-control':'public, max-age=60'}});}}
